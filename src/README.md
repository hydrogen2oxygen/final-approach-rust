# The Backend Server written in Rust
It provides the API for the frontend and serves the static files.

And it also embeds the Angular app as an asset. The same UI will be used for both, the local app and the remote app on an PHP capable server.